admin:
  user: admin
  password: "admin_password" # в проде заменить на Secret или autogenerate

service:
  type: NodePort
  port: 3000
  nodeport: 32001

persistence:
  enabled: true
  existingClaim: grafana-pvc
